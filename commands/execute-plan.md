---
description: "Alias for /implement — executes the latest plan through the relay"
---

`/execute-plan` is an alias for `/implement`.

Tell the user you are running the `/implement` handoff, then follow
`~/.claude/commands/implement.md` exactly, passing `$ARGUMENTS` through unchanged.

Do not maintain a separate execution path here. If the handoff behavior needs to change,
change `implement.md`.
